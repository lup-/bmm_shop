account default
host $SMTP_HOSTNAME
port 587
tls on
tls_starttls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
tls_certcheck on
auth on
user $SMTP_LOGIN
password "$SMTP_PASS"
from "$SMTP_USER"
logfile /var/log/msmtp.log